# 第二部分：讀取你的資料

本部分預計時間：20 分鐘

## 任務 6：讀取 CSV 檔案

📋 **複製這段話，貼給 AI：**

> 我有一個 CSV 檔案叫做 patient_data.csv，放在我的桌面。我正在用 RStudio。請告訴我：
> 
> 1. 怎麼讀取這個檔案到 R 裡面
> 2. 怎麼看前幾筆資料確認有讀成功
> 
> 請給我可以直接執行的程式碼。

### 讀取資料

```{r}
#| label: read-data
#| echo: true

# 讀取 CSV 檔案
my_data <- read.csv("patient_data.csv")

# 顯示前 6 筆資料
head(my_data)
```

## 任務 7：認識你的資料

📋 **複製這段話，貼給 AI：**

> 我已經把資料讀進來了，存在一個叫做 `my_data` 的變數。請給我程式碼，讓我可以：
> 
> 1. 看這個資料有幾列幾欄
> 2. 看每個欄位的基本統計（平均、最大、最小等）
> 3. 看每個欄位是什麼資料類型（數字、文字等）

### 探索資料

```{r}
#| label: explore-data
#| echo: true

# 查看資料維度（幾列幾欄）
dim(my_data)

# 查看資料結構
str(my_data)

# 查看基本統計摘要
summary(my_data)
```

## 任務 8：理解你看到的東西

📋 **把 `summary(my_data)` 的輸出複製起來，加上這段話貼給 AI：**

> 這是我的資料摘要，請幫我解讀每個欄位的意義，以及有沒有什麼需要注意的地方（例如遺漏值、異常值）：
> 【貼上 summary 輸出】

### 資料摘要解讀

從 `summary()` 的輸出，我們可以看到：

- **patient_id**: 病人編號，從 1 到 100
- **treatment**: 治療組別，A 組和 B 組各 50 人
- **age**: 年齡分佈，平均約 47 歲
- **gender**: 性別分佈，男女各半
- **los**: 住院天數，平均約 8 天，B 組似乎住院天數較長

### 檢查遺漏值

```{r}
#| label: check-missing
#| echo: true

# 檢查是否有遺漏值
sum(is.na(my_data))

# 查看每個欄位的遺漏值數量
colSums(is.na(my_data))
```